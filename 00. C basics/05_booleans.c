// booleans are basically yes no in programming language
// in terms of coding yes is true equivalent to 1
// no is false, equivalent to 0
// booleans are 1 bit only

#include <stdlib.h>
#include <stdbool.h>
    // requires stdbool.h

int main(){
    bool yes = true;
    bool no = false;
        // this are useful in conditions
    return 0;
}