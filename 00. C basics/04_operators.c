#include <stdio.h>

int main(){
    int a = 5;
    int b = 2;
    // a+b -> adds
    // a-b -> subtracts
    // a*b -> multiplies
    // a/b -> divides
    // a%b -> remainder
    // a++ -> a = a+1
    // a-- -> a = a-1
    // a+=5 -> a = a+5
    // similar for a-=5, a/=5, a%=5
    // a == b checks if a and b are equal
    // a >= b, a<=b, a>b, a<b etc similarly compares
    
    return 0;
}